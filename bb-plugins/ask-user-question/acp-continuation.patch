diff --git a/apps/server/src/internal/tool-calls.ts b/apps/server/src/internal/tool-calls.ts
index 64a9f5e..1e012e2 100644
--- a/apps/server/src/internal/tool-calls.ts
+++ b/apps/server/src/internal/tool-calls.ts
@@ -86,6 +86,7 @@ export function registerInternalToolCallRoutes(app: Hono, deps: AppDeps): void {
             ctx: {
               threadId: thread.id,
               projectId: thread.projectId,
+              providerId: thread.providerId,
               signal: context.req.raw.signal,
             },
           }),
diff --git a/apps/server/src/services/interactions/pending-interactions.ts b/apps/server/src/services/interactions/pending-interactions.ts
index 57bd573..ef064d7 100644
--- a/apps/server/src/services/interactions/pending-interactions.ts
+++ b/apps/server/src/services/interactions/pending-interactions.ts
@@ -111,6 +111,7 @@ interface RequestPluginInteractionArgs {
   rendererId: string;
   title: string;
   payload: JsonValue;
+  nonBlocking?: boolean;
   timeoutMs: number;
   signal?: AbortSignal;
 }
@@ -269,6 +270,15 @@ function notifyInteractionChanged({
   );
 }
 
+function isNonBlockingPluginInteraction(
+  interaction: PendingInteraction,
+): boolean {
+  return (
+    isPluginPendingInteraction(interaction) &&
+    interaction.payload.nonBlocking === true
+  );
+}
+
 export class PendingInteractionLifecycle {
   private readonly deps: CreateLifecycleDeps;
   private readonly pluginWaiters = new Map<string, PluginInteractionWaiter>();
@@ -348,9 +358,14 @@ export class PendingInteractionLifecycle {
   }
 
   hasPendingThreadInteraction(threadId: string): boolean {
-    return (
-      getActivePendingInteractionForThread(this.deps.db, threadId) !== null
-    );
+    const row = getActivePendingInteractionForThread(this.deps.db, threadId);
+    if (row === null) return false;
+    try {
+      return !isNonBlockingPluginInteraction(toPendingInteraction(row));
+    } catch {
+      // A malformed row must never allow a message to bypass an unknown gate.
+      return true;
+    }
   }
 
   registerPendingInteraction(
@@ -488,6 +503,7 @@ export class PendingInteractionLifecycle {
         payload: JSON.stringify({
           kind: "plugin",
           title: args.title,
+          ...(args.nonBlocking ? { nonBlocking: true } : {}),
           data: args.payload,
         }),
       });
@@ -527,7 +543,7 @@ export class PendingInteractionLifecycle {
       appendPendingInteractionTimelineEvent(this.deps, interaction);
       notifyInteractionChanged({
         deps: this.deps,
-        hasPendingInteraction: true,
+        hasPendingInteraction: !isNonBlockingPluginInteraction(interaction),
         threadId: interaction.threadId,
       });
     } catch (error) {
diff --git a/apps/server/src/services/plugins/plugin-api.ts b/apps/server/src/services/plugins/plugin-api.ts
index a011d04..c046cdc 100644
--- a/apps/server/src/services/plugins/plugin-api.ts
+++ b/apps/server/src/services/plugins/plugin-api.ts
@@ -401,6 +401,7 @@ export function createPluginApi(options: {
     rendererId: string;
     title: string;
     payload: JsonValue;
+    nonBlocking?: boolean;
     timeoutMs: number;
     signal?: AbortSignal;
   }) => Promise<PluginInteractionResult>;
@@ -575,6 +576,7 @@ export function createPluginApi(options: {
       rendererId: request.rendererId,
       title: request.title.trim(),
       payload,
+      nonBlocking: request.nonBlocking === true,
       timeoutMs,
       signal: requestOptions?.signal,
     });
diff --git a/apps/server/test/internal/internal-events-tool-calls.test.ts b/apps/server/test/internal/internal-events-tool-calls.test.ts
index 40146d7..0b38cdf 100644
--- a/apps/server/test/internal/internal-events-tool-calls.test.ts
+++ b/apps/server/test/internal/internal-events-tool-calls.test.ts
@@ -92,13 +92,17 @@ describe("internal event and tool-call routes", () => {
       const toolResult = new Promise<ToolCallResponse>((resolve) => {
         completeTool = resolve;
       });
+      let invokedProviderId: string | null = null;
       setPluginAgentContributions({
         listSkillRootContributions: () => [],
         listAgentTools: () => [],
         listInstructionContributions: () => [],
         findAgentTool: (name) =>
           name === record.name ? { pluginId: "fixture", record } : undefined,
-        invokeAgentTool: () => toolResult,
+        invokeAgentTool: ({ ctx }) => {
+          invokedProviderId = ctx.providerId ?? null;
+          return toolResult;
+        },
         resolveMention: async () => ({ ok: false, error: "unused" }),
       });
 
@@ -116,6 +120,7 @@ describe("internal event and tool-call routes", () => {
         const thread = seedThread(harness.deps, {
           projectId: project.id,
           environmentId: environment.id,
+          providerId: "acp-cursor",
         });
 
         const server = serve({
@@ -156,6 +161,7 @@ describe("internal event and tool-call routes", () => {
           const response = earlyResponse ?? (await responsePromise);
 
           expect(earlyResponse).not.toBeNull();
+          expect(invokedProviderId).toBe("acp-cursor");
           expect(response.status).toBe(200);
           await expect(readJson(response)).resolves.toEqual({
             success: true,
diff --git a/apps/server/test/services/pending-interactions.test.ts b/apps/server/test/services/pending-interactions.test.ts
index 9b0d67d..da17a1b 100644
--- a/apps/server/test/services/pending-interactions.test.ts
+++ b/apps/server/test/services/pending-interactions.test.ts
@@ -65,6 +65,7 @@ function requestPluginInteraction(
   args: {
     threadId: string;
     name?: string;
+    nonBlocking?: boolean;
     signal?: AbortSignal;
   },
 ) {
@@ -75,11 +76,43 @@ function requestPluginInteraction(
     title: "Add secrets",
     payload: { fields: [{ name: args.name ?? "API_KEY" }] },
     timeoutMs: 10_000,
+    ...(args.nonBlocking ? { nonBlocking: true } : {}),
     ...(args.signal ? { signal: args.signal } : {}),
   });
 }
 
 describe("pending interaction lifecycle", () => {
+  it("keeps a non-blocking plugin form visible without gating thread delivery", async () => {
+    await withTestHarness(async (harness) => {
+      const thread = seedPluginInteractionThread(harness.deps, "non-blocking");
+      const pending = requestPluginInteraction(harness.deps, {
+        threadId: thread.id,
+        nonBlocking: true,
+      });
+
+      expect(
+        harness.deps.pendingInteractions.listPendingThreadInteractions(thread.id),
+      ).toMatchObject([
+        {
+          status: "pending",
+          payload: { kind: "plugin", nonBlocking: true },
+        },
+      ]);
+      expect(
+        harness.deps.pendingInteractions.hasPendingThreadInteraction(thread.id),
+      ).toBe(false);
+
+      const [interaction] =
+        harness.deps.pendingInteractions.listPendingThreadInteractions(thread.id);
+      harness.deps.pendingInteractions.cancelPluginInteraction({
+        threadId: thread.id,
+        interactionId: interaction!.id,
+        reason: "user",
+      });
+      await expect(pending).resolves.toEqual({ outcome: "cancelled", reason: "user" });
+    });
+  });
+
   it("returns a plugin response only through memory and persists metadata only", async () => {
     await withTestHarness(async (harness) => {
       const thread = seedPluginInteractionThread(harness.deps, "memory-only");
diff --git a/apps/server/test/services/plugins/plugin-agent-tools.test.ts b/apps/server/test/services/plugins/plugin-agent-tools.test.ts
index 7e972ae..807eb47 100644
--- a/apps/server/test/services/plugins/plugin-agent-tools.test.ts
+++ b/apps/server/test/services/plugins/plugin-agent-tools.test.ts
@@ -176,6 +176,7 @@ describe("bb.agents.registerTool", () => {
     const ctx = {
       threadId: "thr_1",
       projectId: "proj_1",
+      providerId: "codex",
       signal: new AbortController().signal,
     };
     const alpha = service.findAgentTool("alpha_tool")!;
@@ -221,6 +222,7 @@ describe("bb.agents.registerTool", () => {
     const ctx = {
       threadId: "thr_1",
       projectId: "proj_1",
+      providerId: "codex",
       signal: new AbortController().signal,
     };
     const found = service.findAgentTool("search_issues")!;
diff --git a/packages/domain/src/pending-interactions.ts b/packages/domain/src/pending-interactions.ts
index 06e87b8..d4378f4 100644
--- a/packages/domain/src/pending-interactions.ts
+++ b/packages/domain/src/pending-interactions.ts
@@ -339,6 +339,7 @@ export type UserQuestionPendingInteractionPayload = z.infer<
 const pluginPendingInteractionPayloadSchema = z.object({
   kind: z.literal("plugin"),
   title: z.string().trim().min(1).max(PLUGIN_INTERACTION_MAX_TITLE_LENGTH),
+  nonBlocking: z.boolean().optional(),
   data: jsonValueSchema,
 });
 type PluginPendingInteractionPayload = z.infer<
diff --git a/packages/plugin-sdk/src/backend-contract.ts b/packages/plugin-sdk/src/backend-contract.ts
index ee8f3b2..2a3888a 100644
--- a/packages/plugin-sdk/src/backend-contract.ts
+++ b/packages/plugin-sdk/src/backend-contract.ts
@@ -621,6 +621,12 @@ export interface PluginInteractionRequest {
   rendererId: string;
   title: string;
   payload: JsonValue;
+  /**
+   * Keep this form visible without treating it as a thread-level dispatch
+   * barrier. The plugin remains responsible for delivering submitted values.
+   * This is intended for asynchronous inbox-style prompts, not approvals.
+   */
+  nonBlocking?: boolean;
   /** Defaults to ten minutes; capped at one hour. */
   timeoutMs?: number;
 }
@@ -696,6 +702,8 @@ export type PluginAgentToolResult =
 export interface PluginAgentToolContext {
   threadId: string;
   projectId: string;
+  /** Available on hosts that expose provider identity to tool calls. */
+  providerId?: string;
   /** The tool-call request's abort signal (aborts if the daemon round-trip
    * is torn down mid-call). */
   signal: AbortSignal;
diff --git a/packages/plugin-sdk/src/testing/fake-plugin-host.ts b/packages/plugin-sdk/src/testing/fake-plugin-host.ts
index 117292b..11269fc 100644
--- a/packages/plugin-sdk/src/testing/fake-plugin-host.ts
+++ b/packages/plugin-sdk/src/testing/fake-plugin-host.ts
@@ -2247,6 +2247,7 @@ function createFakePluginHostInternal(
       return record.execute(parsed.value, {
         threadId: ctx?.threadId ?? "thread-test",
         projectId: ctx?.projectId ?? "project-test",
+        providerId: ctx?.providerId ?? "codex",
         signal: ctx?.signal ?? new AbortController().signal,
       });
     },
diff --git a/plugins/ask-user-question/app.test.tsx b/plugins/ask-user-question/app.test.tsx
index ad02532..62f8195 100644
--- a/plugins/ask-user-question/app.test.tsx
+++ b/plugins/ask-user-question/app.test.tsx
@@ -54,20 +54,25 @@ function render(
   handlers: {
     submit?: (value: unknown) => Promise<void>;
     cancel?: () => Promise<void>;
+    rpc?: Record<string, (input: unknown) => unknown>;
   } = {},
 ) {
-  return renderSlot(app.pendingInteractions[0]!, {
-    interaction: {
-      id: "pint_test",
-      threadId: "thr_test",
-      title: "Database",
-      payload: payload as never,
-      createdAt: 0,
-      expiresAt: null,
+  return renderSlot(
+    app.pendingInteractions[0]!,
+    {
+      interaction: {
+        id: "pint_test",
+        threadId: "thr_test",
+        title: "Database",
+        payload: payload as never,
+        createdAt: 0,
+        expiresAt: null,
+      },
+      submit: handlers.submit ?? (async () => undefined),
+      cancel: handlers.cancel ?? (async () => undefined),
     },
-    submit: handlers.submit ?? (async () => undefined),
-    cancel: handlers.cancel ?? (async () => undefined),
-  });
+    { rpc: handlers.rpc },
+  );
 }
 
 function getButtonByText(
@@ -216,6 +221,47 @@ describe("multi-select and multi-question flows", () => {
     fireEvent.click(getButtonByText(slot, "Cancel"));
     expect(cancel).toHaveBeenCalledTimes(1);
   });
+
+  it("sends one queued ACP answer without closing the remaining inbox", async () => {
+    const send = vi.fn(async () => ({ remaining: 1, version: 2 }));
+    const slot = render(singleSelect, {
+      submit: vi.fn(async () => undefined),
+      rpc: {
+        getQuestionQueue: () => ({ payload: multi, version: 1 }),
+        answerQueuedQuestion: send,
+      },
+    });
+
+    await slot.findByText("1 of 2");
+    fireEvent.click(getButtonByText(slot, "Metrics"));
+    fireEvent.click(getButtonByText(slot, "Send answer"));
+
+    await vi.waitFor(() =>
+      expect(send).toHaveBeenCalledWith({
+        threadId: "thr_test",
+        questionId: "q0",
+        answer: { selected: ["q0o0"] },
+      }),
+    );
+  });
+
+  it("closes the display-only interaction after the final queued answer", async () => {
+    const cancel = vi.fn(async () => undefined);
+    const send = vi.fn(async () => ({ remaining: 0, version: 2 }));
+    const slot = render(singleSelect, {
+      cancel,
+      rpc: {
+        getQuestionQueue: () => ({ payload: multi, version: 1 }),
+        answerQueuedQuestion: send,
+      },
+    });
+
+    await slot.findByText("1 of 2");
+    fireEvent.click(getButtonByText(slot, "Metrics"));
+    fireEvent.click(getButtonByText(slot, "Send answer"));
+
+    await vi.waitFor(() => expect(cancel).toHaveBeenCalledTimes(1));
+  });
 });
 
 describe("a payload the form cannot read", () => {
diff --git a/plugins/ask-user-question/app.tsx b/plugins/ask-user-question/app.tsx
index 4e03f6a..c06b4ed 100644
--- a/plugins/ask-user-question/app.tsx
+++ b/plugins/ask-user-question/app.tsx
@@ -10,8 +10,11 @@ import {
 } from "react";
 import {
   definePluginApp,
+  useRealtime,
+  useRpc,
   type PluginPendingInteractionProps,
 } from "@get-bb/plugin-sdk/app";
+import type { questionQueueRpcContract } from "./src/server.js";
 import { Button } from "@bb/shared-ui/button";
 import { Icon } from "@bb/shared-ui/icon";
 import { usePointerCoarse } from "@bb/shared-ui/hooks/use-pointer-coarse";
@@ -307,11 +310,36 @@ function AskUserQuestionInteraction({
   submit,
   cancel,
 }: PluginPendingInteractionProps) {
+  const rpc = useRpc<typeof questionQueueRpcContract>();
   const parsed = useMemo(
     () => interactionPayloadSchema.safeParse(interaction.payload),
     [interaction.payload],
   );
-  const questions = parsed.success ? parsed.data.questions : [];
+  const initialQuestions = parsed.success ? parsed.data.questions : [];
+  const [queuedQuestions, setQueuedQuestions] = useState<
+    readonly InteractionQuestion[] | null
+  >(null);
+  const [queueError, setQueueError] = useState<string | null>(null);
+  const [sendError, setSendError] = useState<string | null>(null);
+  const refreshQueue = useCallback(() => {
+    void rpc
+      .call("getQuestionQueue", { threadId: interaction.threadId })
+      .then((result) => {
+        setQueuedQuestions(result.payload?.questions ?? null);
+        setQueueError(null);
+      })
+      .catch((error: unknown) => {
+        setQueueError(
+          `Could not load this question: ${
+            error instanceof Error ? error.message : String(error)
+          }`,
+        );
+      });
+  }, [interaction.threadId, rpc]);
+  useEffect(refreshQueue, [refreshQueue]);
+  useRealtime("question-queue-changed", refreshQueue);
+  const questions = queuedQuestions ?? initialQuestions;
+  const queued = queuedQuestions !== null;
   const [formState, setFormState] = useState<QuestionFormState>(() =>
     createInitialFormState(questions),
   );
@@ -340,6 +368,12 @@ function AskUserQuestionInteraction({
     [formState, questions, totalQuestions],
   );
 
+  useEffect(() => {
+    setCurrentIndex((index) =>
+      Math.min(index, Math.max(totalQuestions - 1, 0)),
+    );
+  }, [totalQuestions]);
+
   const updateQuestionState = (
     question: InteractionQuestion,
     update: (state: QuestionAnswerState) => QuestionAnswerState,
@@ -383,6 +417,7 @@ function AskUserQuestionInteraction({
   const submitAnswer = (): void => {
     if (busy || !allAnswered) return;
     setBusy(true);
+    setSendError(null);
     void (async () => {
       try {
         await submit(buildInteractionResponse(questions, formState));
@@ -393,7 +428,50 @@ function AskUserQuestionInteraction({
     })();
   };
 
+  const sendCurrentAnswer = (): void => {
+    if (busy || currentQuestion === null) return;
+    if (
+      !isQuestionAnswered(
+        currentQuestion,
+        answerStateFor(formState, currentQuestion),
+      )
+    ) {
+      return;
+    }
+    const answer = buildInteractionResponse([currentQuestion], formState)
+      .answers[currentQuestion.id];
+    if (answer === undefined) return;
+    setBusy(true);
+    void rpc
+      .call("answerQueuedQuestion", {
+        threadId: interaction.threadId,
+        questionId: currentQuestion.id,
+        answer,
+      })
+      .then((result) => {
+        if (result.remaining === 0) {
+          // The answer is already on its way to the agent. Settle the
+          // display-only interaction so its card and server row do not linger.
+          void cancel();
+          return;
+        }
+        refreshQueue();
+      })
+      .catch((error: unknown) =>
+        setSendError(
+          `Could not send answer: ${
+            error instanceof Error ? error.message : String(error)
+          }`,
+        ),
+      )
+      .finally(() => setBusy(false));
+  };
+
   const handleAdvance = (): void => {
+    if (queued) {
+      sendCurrentAnswer();
+      return;
+    }
     if (isLast) {
       submitAnswer();
       return;
@@ -455,7 +533,16 @@ function AskUserQuestionInteraction({
     );
   }
 
-  if (!currentQuestion) return null;
+  if (!currentQuestion) {
+    return queued ? (
+      <div className="flex items-center justify-between gap-2 text-sm text-muted-foreground">
+        <span>No questions waiting.</span>
+        <Button type="button" size="sm" variant="ghost" onClick={handleCancel}>
+          Close
+        </Button>
+      </div>
+    ) : null;
+  }
 
   const currentState = answerStateFor(formState, currentQuestion);
 
@@ -509,16 +596,23 @@ function AskUserQuestionInteraction({
           <Button
             type="button"
             size="sm"
-            disabled={busy || (isLast && !allAnswered)}
+            disabled={
+              busy ||
+              (queued
+                ? !isQuestionAnswered(currentQuestion, currentState)
+                : isLast && !allAnswered)
+            }
             onClick={handleAdvance}
           >
             {busy ? (
               <Icon name="Spinner" className="size-3 animate-spin" />
             ) : null}
-            {isLast ? "Submit answer" : "Next"}
+            {queued ? "Send answer" : isLast ? "Submit answer" : "Next"}
           </Button>
         </div>
       </div>
+      {queueError !== null ? <p role="alert">{queueError}</p> : null}
+      {sendError !== null ? <p role="alert">{sendError}</p> : null}
     </div>
   );
 }
diff --git a/plugins/ask-user-question/src/server-harness.test.ts b/plugins/ask-user-question/src/server-harness.test.ts
index bf0dc30..b1eba03 100644
--- a/plugins/ask-user-question/src/server-harness.test.ts
+++ b/plugins/ask-user-question/src/server-harness.test.ts
@@ -3,17 +3,48 @@ import {
   createFakePluginHost,
   type FakePluginHost,
 } from "@get-bb/plugin-sdk/testing";
-import type { PluginAgentConfigurationContext } from "@get-bb/plugin-sdk";
+import type {
+  PluginAgentConfigurationContext,
+  PluginAgentToolContext,
+} from "@get-bb/plugin-sdk";
 import plugin, { RENDERER_ID, TOOL_NAME } from "./server.js";
 import { TOOL_INPUT_JSON_SCHEMA } from "./tool-definition.js";
 import type { InteractionPayload, ToolResult } from "./contracts.js";
 
-function createHost(): FakePluginHost {
-  const host = createFakePluginHost({ pluginId: "ask-user-question" });
+function createHost(options?: {
+  send?: (args: unknown) => Promise<unknown>;
+  providerId?: string;
+}): FakePluginHost {
+  const host = createFakePluginHost({
+    pluginId: "ask-user-question",
+    sdk: {
+      threads: {
+        ...(options?.send === undefined ? {} : { send: options.send }),
+        ...(options?.providerId === undefined
+          ? {}
+          : { get: async () => ({ providerId: options.providerId }) }),
+      },
+    },
+  });
   plugin(host.bb as unknown as Parameters<typeof plugin>[0]);
   return host;
 }
 
+async function callWithoutProviderId(
+  host: FakePluginHost,
+  input: unknown,
+): Promise<unknown> {
+  const tool = host.harness.registrations.agentTools.find(
+    (candidate) => candidate.name === TOOL_NAME,
+  );
+  if (tool === undefined) throw new Error("AskUserQuestion is not registered");
+  return tool.execute(input, {
+    threadId: "thread-test",
+    projectId: "project-test",
+    signal: new AbortController().signal,
+  } as unknown as PluginAgentToolContext);
+}
+
 function configurationContext(
   providerId: string,
   supportsNativeUserQuestion = false,
@@ -85,7 +116,7 @@ describe("provider gating", () => {
     },
   );
 
-  it.each(["codex", "pi", "acp-cursor"])(
+  it.each(["codex", "pi", "acp-cursor", "acp-opencode", "acp-custom"])(
     "registers the tool for %s with Claude's exact advertised schema",
     async (providerId) => {
       const host = createHost();
@@ -96,6 +127,14 @@ describe("provider gating", () => {
       const [tool] = resolved.tools;
       expect(tool?.name).toBe(TOOL_NAME);
       expect(tool?.inputSchema).toEqual(TOOL_INPUT_JSON_SCHEMA);
+      expect(tool?.description).toContain(
+        "Batch 1-4 independent questions in a single call",
+      );
+      expect(resolved.instructions).toBe(
+        providerId.startsWith("acp-")
+          ? "On ACP, AskUserQuestion adds each call to the visible question inbox and returns immediately. Continue independent work; each submitted answer arrives as a follow-up message."
+          : null,
+      );
     },
   );
 
@@ -126,6 +165,139 @@ describe("provider gating", () => {
 });
 
 describe("asking a question", () => {
+  it("returns more than one interaction's queued questions through the RPC", async () => {
+    const host = createHost({
+      providerId: "acp-cursor",
+      send: async () => ({}),
+    });
+
+    for (let index = 0; index < 5; index += 1) {
+      await callWithoutProviderId(host, {
+        questions: [
+          {
+            ...questions[0],
+            question: `Which database should we use for ${index}?`,
+            header: `Database ${index}`,
+          },
+        ],
+      });
+    }
+
+    const queued = (await host.harness.callRpc("getQuestionQueue", {
+      threadId: "thread-test",
+    })) as { payload: { questions: Array<{ id: string }> } | null };
+    expect(queued.payload?.questions).toHaveLength(5);
+    expect(queued.payload?.questions.map((question) => question.id)).toEqual([
+      "q0",
+      "q1",
+      "q2",
+      "q3",
+      "q4",
+    ]);
+  });
+
+  it("uses the thread provider when the live tool context omits providerId", async () => {
+    const host = createHost({
+      providerId: "acp-cursor",
+      send: async () => ({}),
+    });
+    const followUp = {
+      ...questions[0],
+      question: "Which cache should we use?",
+      header: "Cache",
+    };
+
+    await expect(callWithoutProviderId(host, { questions })).resolves.toBe(
+      "Question added. You may continue independent work; each user answer arrives as a follow-up message.",
+    );
+    await expect(
+      callWithoutProviderId(host, { questions: [followUp] }),
+    ).resolves.toBe(
+      "Question added. You may continue independent work; each user answer arrives as a follow-up message.",
+    );
+    expect(host.harness.pendingInteractions).toHaveLength(1);
+
+    const before = await host.harness.callRpc("getQuestionQueue", {
+      threadId: "thread-test",
+    });
+    expect(before).toMatchObject({
+      payload: { questions: [{ id: "q0" }, { id: "q1" }] },
+    });
+
+    await host.harness.callRpc("answerQueuedQuestion", {
+      threadId: "thread-test",
+      questionId: "q0",
+      answer: { selected: ["q0o1"] },
+    });
+    expect(host.harness.sdk.callsTo("threads.send")).toHaveLength(1);
+    await expect(
+      host.harness.callRpc("getQuestionQueue", {
+        threadId: "thread-test",
+      }),
+    ).resolves.toMatchObject({ payload: { questions: [{ id: "q1" }] } });
+  });
+
+  it.each(["acp-cursor", "acp-opencode", "acp-custom"])(
+    "returns immediately for %s and sends its later answer to the thread",
+    async (providerId) => {
+      const host = createHost({ send: async () => ({}) });
+      const controller = new AbortController();
+
+      const result = await host.harness.callAgentTool(
+        TOOL_NAME,
+        { questions },
+        { providerId, signal: controller.signal } as never,
+      );
+
+    expect(await resultText(result)).toBe(
+      "Question added. You may continue independent work; each user answer arrives as a follow-up message.",
+    );
+    expect(host.harness.pendingInteractions).toHaveLength(1);
+    expect(host.harness.pendingInteractions[0]?.nonBlocking).toBe(true);
+    controller.abort();
+      expect(host.harness.pendingInteractions).toHaveLength(1);
+      const pending = host.harness.pendingInteractions[0]!;
+      host.harness.submitInteraction(pending.id, {
+        answers: { q0: { selected: ["q0o1"] } },
+      });
+
+      await vi.waitFor(() =>
+        expect(host.harness.sdk.callsTo("threads.send")).toEqual([
+          [
+            {
+              threadId: "thread-test",
+              mode: "auto",
+              input: [
+                {
+                  type: "text",
+                  text: expect.stringContaining(
+                    '"Which database should we use?":"SQLite"',
+                  ),
+                  mentions: [],
+                  visibility: "agent-only",
+                },
+              ],
+            },
+          ],
+        ]),
+      );
+    },
+  );
+
+  it("does not send a follow-up when an ACP question is dismissed", async () => {
+    const host = createHost({ send: async () => ({}) });
+    await host.harness.callAgentTool(
+      TOOL_NAME,
+      { questions },
+      { providerId: "acp-cursor" } as never,
+    );
+
+    host.harness.cancelInteraction(host.harness.pendingInteractions[0]!.id);
+    await Promise.resolve();
+
+    expect(host.harness.sdk.callsTo("threads.send")).toEqual([]);
+  });
+
   it("opens an interaction and returns the answer in Claude's result shape", async () => {
     const host = createHost();
     const call = host.harness.callAgentTool(TOOL_NAME, { questions });
diff --git a/plugins/ask-user-question/src/server.ts b/plugins/ask-user-question/src/server.ts
index 892065a..04afc41 100644
--- a/plugins/ask-user-question/src/server.ts
+++ b/plugins/ask-user-question/src/server.ts
@@ -1,8 +1,16 @@
-import type { BbPluginApi, PluginAgentToolResult } from "@get-bb/plugin-sdk";
+import {
+  defineRpcContract,
+  type BbPluginApi,
+  type PluginAgentToolResult,
+} from "@get-bb/plugin-sdk";
+import { z } from "zod";
 import {
   ASK_USER_QUESTION_RENDERER_ID,
+  interactionPayloadSchema,
   interactionResponseSchema,
   toolInputSchema,
+  type InteractionPayload,
+  type InteractionQuestion,
 } from "./contracts.js";
 import {
   TOOL_DESCRIPTION,
@@ -20,11 +28,223 @@ import {
 export const TOOL_NAME = "AskUserQuestion";
 export const RENDERER_ID = ASK_USER_QUESTION_RENDERER_ID;
 
+const ACP_PROVIDER_ID_PREFIX = "acp-";
+const QUESTION_QUEUE_CHANNEL = "question-queue-changed";
+const MAX_QUEUED_QUESTIONS = 32;
+const ACP_PENDING_RESULT =
+  "Question added. You may continue independent work; each user answer arrives as a follow-up message.";
+
+interface QuestionQueue {
+  questions: InteractionQuestion[];
+  nextQuestionId: number;
+  version: number;
+}
+
+const queuedInteractionPayloadSchema = z.object({
+  questions: z
+    .array(interactionPayloadSchema.shape.questions.element)
+    .min(1)
+    .max(MAX_QUEUED_QUESTIONS),
+});
+
+export const questionQueueRpcContract = defineRpcContract({
+  getQuestionQueue: {
+    input: z.object({ threadId: z.string().min(1) }).strict(),
+    output: z
+      .object({
+        payload: queuedInteractionPayloadSchema.nullable(),
+        version: z.number().int().nonnegative(),
+      })
+      .strict(),
+  },
+  answerQueuedQuestion: {
+    input: z
+      .object({
+        threadId: z.string().min(1),
+        questionId: z.string().min(1),
+        answer: z.unknown(),
+      })
+      .strict(),
+    output: z
+      .object({
+        remaining: z.number().int().nonnegative(),
+        version: z.number().int().nonnegative(),
+      })
+      .strict(),
+  },
+});
+
 function errorResult(message: string): PluginAgentToolResult {
   return { content: [{ type: "text", text: message }], isError: true };
 }
 
+function isAcpProviderId(providerId: unknown): boolean {
+  return (
+    typeof providerId === "string" &&
+    providerId.startsWith(ACP_PROVIDER_ID_PREFIX)
+  );
+}
+
+async function resolveProviderId(
+  bb: BbPluginApi,
+  ctx: { threadId: string; providerId?: unknown },
+): Promise<string | null> {
+  if (typeof ctx.providerId === "string") return ctx.providerId;
+  try {
+    const thread = await bb.sdk.threads.get({ threadId: ctx.threadId });
+    const providerId = (thread as { providerId?: unknown }).providerId;
+    return typeof providerId === "string" ? providerId : null;
+  } catch (error) {
+    bb.log.warn(
+      `AskUserQuestion could not resolve the thread provider: ${
+        error instanceof Error ? error.message : String(error)
+      }`,
+    );
+    return null;
+  }
+}
+
+function appendQuestions(
+  queue: QuestionQueue,
+  payload: InteractionPayload,
+): void {
+  const seenPrompts = new Set(
+    queue.questions.map((question) => question.prompt),
+  );
+  for (const source of payload.questions) {
+    if (seenPrompts.has(source.prompt)) continue;
+    if (queue.questions.length >= MAX_QUEUED_QUESTIONS) break;
+    const id = `q${queue.nextQuestionId++}`;
+    queue.questions.push({
+      ...source,
+      id,
+      options: source.options.map((option, index) => ({
+        ...option,
+        value: `${id}o${index}`,
+      })),
+    });
+    seenPrompts.add(source.prompt);
+  }
+  queue.version += 1;
+}
+
+async function deliverAcpAnswer(args: {
+  bb: BbPluginApi;
+  threadId: string;
+  payload: ReturnType<typeof buildInteractionPayload>;
+  result: Awaited<ReturnType<BbPluginApi["ui"]["requestInput"]>>;
+}): Promise<void> {
+  const { bb, threadId, payload, result } = args;
+  if (result.outcome !== "submitted") return;
+
+  const parsed = interactionResponseSchema.safeParse(result.value);
+  if (!parsed.success) {
+    bb.log.warn("AskUserQuestion could not read an ACP answer");
+    return;
+  }
+  const toolResult = buildToolResult(payload, parsed.data);
+  if (Object.keys(toolResult.answers).length === 0) {
+    bb.log.info("AskUserQuestion ACP answer was empty");
+    return;
+  }
+
+  try {
+    await bb.sdk.threads.send({
+      threadId,
+      mode: "auto",
+      input: [
+        {
+          type: "text",
+          text: `AskUserQuestion answer:\n${JSON.stringify(toolResult)}`,
+          mentions: [],
+          visibility: "agent-only",
+        },
+      ],
+    });
+  } catch (error) {
+    bb.log.warn(
+      `Could not deliver AskUserQuestion answer to ACP: ${
+        error instanceof Error ? error.message : String(error)
+      }`,
+    );
+  }
+}
+
 export default function plugin(bb: BbPluginApi) {
+  const queues = new Map<string, QuestionQueue>();
+  const sendingQuestionIds = new Set<string>();
+  const publishQueue = (
+    threadId: string,
+    queue: QuestionQueue | undefined,
+  ): void => {
+    bb.realtime.publish(QUESTION_QUEUE_CHANNEL, {
+      threadId,
+      version: queue?.version ?? 0,
+    });
+  };
+
+  bb.rpc.register(questionQueueRpcContract, {
+    getQuestionQueue({ threadId }) {
+      const queue = queues.get(threadId);
+      return {
+        payload:
+          queue === undefined || queue.questions.length === 0
+            ? null
+            : { questions: queue.questions },
+        version: queue?.version ?? 0,
+      };
+    },
+    async answerQueuedQuestion({ threadId, questionId, answer }) {
+      const queue = queues.get(threadId);
+      const question = queue?.questions.find(
+        (candidate) => candidate.id === questionId,
+      );
+      const parsed = interactionResponseSchema.safeParse({
+        answers: { [questionId]: answer },
+      });
+      if (queue === undefined || question === undefined || !parsed.success)
+        throw new Error("Question answer is no longer pending.");
+      const toolResult = buildToolResult(
+        { questions: [question] },
+        parsed.data,
+      );
+      if (Object.keys(toolResult.answers).length === 0)
+        throw new Error("Choose an answer before sending it.");
+      const sendingKey = `${threadId}:${questionId}`;
+      if (sendingQuestionIds.has(sendingKey)) {
+        throw new Error("That answer is already being delivered.");
+      }
+      sendingQuestionIds.add(sendingKey);
+      try {
+        await bb.sdk.threads.send({
+          threadId,
+          mode: "auto",
+          input: [
+            {
+              type: "text",
+              text: `AskUserQuestion answer:\n${JSON.stringify(toolResult)}`,
+              mentions: [],
+              visibility: "agent-only",
+            },
+          ],
+        });
+        queue.questions = queue.questions.filter(
+          (candidate) => candidate.id !== questionId,
+        );
+        queue.version += 1;
+        if (queue.questions.length === 0) {
+          queues.delete(threadId);
+          publishQueue(threadId, undefined);
+        } else {
+          publishQueue(threadId, queue);
+        }
+      } finally {
+        sendingQuestionIds.delete(sendingKey);
+      }
+      return { remaining: queue.questions.length, version: queue.version };
+    },
+  });
+
   bb.agents.registerTool({
     name: TOOL_NAME,
     description: TOOL_DESCRIPTION,
@@ -48,17 +268,54 @@ export default function plugin(bb: BbPluginApi) {
       }
 
       const askedAt = Date.now();
+      const acp = isAcpProviderId(await resolveProviderId(bb, ctx));
       let result;
       try {
-        result = await bb.ui.requestInput(
+        const existing = acp ? queues.get(ctx.threadId) : undefined;
+        if (existing !== undefined) {
+          appendQuestions(existing, payload);
+          publishQueue(ctx.threadId, existing);
+          return ACP_PENDING_RESULT;
+        }
+        const queue = acp
+          ? { questions: [], nextQuestionId: 0, version: 0 }
+          : undefined;
+        if (queue !== undefined) {
+          appendQuestions(queue, payload);
+          queues.set(ctx.threadId, queue);
+        }
+        const request = bb.ui.requestInput(
           {
             threadId: ctx.threadId,
             rendererId: RENDERER_ID,
             title: buildInteractionTitle(payload),
             payload,
+            nonBlocking: acp,
           },
-          { signal: ctx.signal },
+          acp ? undefined : { signal: ctx.signal },
         );
+        if (acp) {
+          void request.then(
+            (resolved) => (
+              queues.delete(ctx.threadId),
+              publishQueue(ctx.threadId, undefined),
+              deliverAcpAnswer({
+                bb,
+                threadId: ctx.threadId,
+                payload,
+                result: resolved,
+              })
+            ),
+            (error) =>
+              bb.log.warn(
+                `AskUserQuestion could not wait for an ACP answer: ${
+                  error instanceof Error ? error.message : String(error)
+                }`,
+              ),
+          );
+          return ACP_PENDING_RESULT;
+        }
+        result = await request;
       } catch (error) {
         return errorResult(
           `The question could not be shown (${error instanceof Error ? error.message : String(error)}). Only one prompt can await the user at a time — put all of your questions in a single AskUserQuestion call, or continue with your best judgement.`,
@@ -93,9 +350,13 @@ export default function plugin(bb: BbPluginApi) {
     if (context.provider.capabilities.supportsNativeUserQuestion) {
       return { tools: [], skills: [] };
     }
+    const instructions = isAcpProviderId(context.provider.id)
+      ? "On ACP, AskUserQuestion adds each call to the visible question inbox and returns immediately. Continue independent work; each submitted answer arrives as a follow-up message."
+      : undefined;
     return {
       tools: [{ name: TOOL_NAME, parameters: TOOL_INPUT_JSON_SCHEMA }],
       skills: [],
+      ...(instructions === undefined ? {} : { instructions }),
     };
   });
 }
diff --git a/plugins/ask-user-question/src/tool-definition.ts b/plugins/ask-user-question/src/tool-definition.ts
index 828fdeb..02eeaff 100644
--- a/plugins/ask-user-question/src/tool-definition.ts
+++ b/plugins/ask-user-question/src/tool-definition.ts
@@ -2,6 +2,8 @@ export const TOOL_DESCRIPTION = `Use this tool only when you are blocked on a de
 
 Usage notes:
 - Users will always be able to select "Other" to provide custom text input
+- Batch 1-4 independent questions in a single call: the user sees one form and the response includes every answer
+- On ACP, later AskUserQuestion calls join the open question inbox; each answer is delivered separately while unanswered questions remain visible
 - Use multiSelect: true to allow multiple answers to be selected for a question
 - If you recommend a specific option, make that the first option in the list and add "(Recommended)" at the end of the label
 
