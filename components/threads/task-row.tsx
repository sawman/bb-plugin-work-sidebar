import { useRef, type DragEvent, type MouseEvent as ReactMouseEvent } from "react";
import { ContextMenu, ContextMenuContent, ContextMenuItem, ContextMenuLabel, ContextMenuSeparator, ContextMenuTrigger } from "../ui/context-menu";
import { Icon, type IconName } from "../ui/icon";
import type { SidebarTask, TaskQueueNode } from "../../work-model";

const TASK_STATUSES: readonly SidebarTask["status"][] = ["backlog", "todo", "in_progress", "in_review", "done", "canceled"];
const taskStatus = (status: SidebarTask["status"]): { label: string; icon: IconName } => ({ backlog: { label: "Backlog", icon: "ListTodo" }, todo: { label: "To do", icon: "Circle" }, in_progress: { label: "In progress", icon: "LoaderCircle" }, in_review: { label: "In review", icon: "Eye" }, done: { label: "Done", icon: "Check" }, canceled: { label: "Canceled", icon: "X" } })[status];

export type TaskRowProps = { node: TaskQueueNode; siblings: readonly TaskQueueNode[]; showProject: boolean; reorderDisabled: boolean; dragTaskId: string | null; dropTarget: { taskId: string; placement: "before" | "after" } | null; onDragTaskChange(taskId: string | null): void; onDragTargetChange(taskId: string | null, placement?: "before" | "after"): void; onDropTask(sourceId: string, targetId: string, placement: "before" | "after"): void; onMoveTask(taskId: string, direction: -1 | 1): void; onOpenThread(threadId: string, split?: boolean): void; onUpdateStatus(taskId: string, status: SidebarTask["status"]): Promise<void>; updatingTaskId: string | null; selectedTaskIds: ReadonlySet<string>; onSelect(taskId: string, event: ReactMouseEvent<HTMLButtonElement>): boolean; };

export function TaskRow(props: TaskRowProps) {
  const { node, siblings, showProject, reorderDisabled, dragTaskId, dropTarget, onDragTaskChange, onDragTargetChange, onDropTask, onMoveTask, onOpenThread, onUpdateStatus, updatingTaskId, selectedTaskIds, onSelect } = props;
  const { task } = node;
  const controlClick = useRef(false);
  const workers = task.linkedThreadIds;
  const status = taskStatus(task.status);
  const peers = siblings.filter((candidate) => candidate.task.projectId === task.projectId && candidate.task.status === task.status && candidate.task.parentTaskId === task.parentTaskId);
  const index = peers.findIndex((candidate) => candidate.task.id === task.id);
  const interactive = (event: DragEvent<HTMLElement>) => Boolean((event.target as HTMLElement).closest("button,a,summary,[role=menu],[role=menuitem]"));
  const open = () => workers[0] && onOpenThread(workers[0], workers.length === 1);
  const placement = (event: DragEvent<HTMLElement>): "before" | "after" => { const bounds = event.currentTarget.getBoundingClientRect(); return event.clientY > bounds.top + bounds.height / 2 ? "after" : "before"; };
  return <ContextMenu><ContextMenuTrigger asChild><article className={`ws-task-row ws-task-row-${node.role} ${selectedTaskIds.has(task.id) ? "ws-task-row-selected" : ""} ${dragTaskId === task.id ? "ws-task-dragging" : ""}`} aria-selected={selectedTaskIds.has(task.id)} data-task-role={node.role} data-task-id={task.id} data-drop-placement={dropTarget?.taskId === task.id ? dropTarget.placement : undefined} draggable={!reorderDisabled} onDragStart={(event) => { if (reorderDisabled || interactive(event)) { event.preventDefault(); return; } event.dataTransfer.effectAllowed = "move"; event.dataTransfer.setData("text/plain", task.id); onDragTaskChange(task.id); }} onDragOver={(event) => { if (reorderDisabled || !dragTaskId || dragTaskId === task.id || !peers.some((candidate) => candidate.task.id === dragTaskId)) return; event.preventDefault(); onDragTargetChange(task.id, placement(event)); }} onDragLeave={(event) => { if (!event.currentTarget.contains(event.relatedTarget as Node)) onDragTargetChange(null); }} onDrop={(event) => { if (!dragTaskId || dragTaskId === task.id) return; event.preventDefault(); onDropTask(dragTaskId, task.id, placement(event)); onDragTaskChange(null); onDragTargetChange(null); }} onDragEnd={() => { onDragTaskChange(null); onDragTargetChange(null); }}><div className="ws-task-row-main"><button className="ws-task-open" type="button" onMouseDown={(event) => { controlClick.current = event.ctrlKey && event.button === 0; }} onClick={(event) => { if (!onSelect(task.id, event)) open(); }} onContextMenu={(event) => { if (!controlClick.current && !event.ctrlKey) return; controlClick.current = false; event.preventDefault(); onSelect(task.id, event); }} aria-disabled={!workers.length} aria-label={`Open ${task.key} in Tasks${workers.length === 1 ? " and its worker" : ""}`}><strong className="ws-task-title" title={task.title}>{task.title}</strong></button><div className="ws-task-meta"><span className="ws-task-key-badge">{task.key}</span><span className={`ws-task-badge ws-task-priority-${task.priority}`}>{task.priority}</span>{task.dueDate && <span className="ws-task-badge">Due {task.dueDate}</span>}{workers.length > 0 && <span className="ws-task-badge">{workers.length} worker{workers.length === 1 ? "" : "s"}</span>}{showProject && <span className="ws-task-badge">{task.projectName}</span>}</div></div><div className="ws-task-actions"><label className={`ws-task-status-picker ws-task-status-${task.status}`}><Icon name={status.icon} aria-hidden /><span className="sr-only">Change status for {task.key}</span><select value={task.status} disabled={updatingTaskId === task.id} aria-label={`Change status for ${task.key}: ${status.label}`} onChange={(event) => void onUpdateStatus(task.id, event.target.value as SidebarTask["status"])}>{TASK_STATUSES.map((next) => <option key={next} value={next}>{taskStatus(next).label}</option>)}</select></label></div>{node.children.length > 0 && <div className="ws-task-children" aria-label={`Execution tasks for ${task.title}`}>{node.children.map((child) => <TaskRow key={child.id} {...props} node={{ task: child, role: "execution", children: [], hasVisibleOutcomeParent: true }} siblings={node.children.map((childTask) => ({ task: childTask, role: "execution" as const, children: [], hasVisibleOutcomeParent: true }))} />)}</div>}</article></ContextMenuTrigger><ContextMenuContent aria-label={`Actions for ${task.key}`}><ContextMenuLabel>{task.title}</ContextMenuLabel><ContextMenuItem disabled={!workers.length} onSelect={open}>Open</ContextMenuItem><ContextMenuSeparator /><ContextMenuItem disabled={reorderDisabled || index <= 0} onSelect={() => onMoveTask(task.id, -1)}>Move up</ContextMenuItem><ContextMenuItem disabled={reorderDisabled || index < 0 || index >= peers.length - 1} onSelect={() => onMoveTask(task.id, 1)}>Move down</ContextMenuItem></ContextMenuContent></ContextMenu>;
}
